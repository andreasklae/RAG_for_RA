# Example environment variables (do NOT commit real secrets)
#
# Required for running:
#   OPENAI_API_KEY is prompted for if missing
#
# Required to connect to a specific Vector Store:
OPENAI_VECTOR_STORE_ID="vs_..."


